#include <iostream>
#include <string>

using namespace std;

struct Node
{
    int data;
    Node *next;
}

typedef struct Node *stack;

bool isEmpty(stack s)
{
    return (s == null)
}

int main()
{
    return 0;
}